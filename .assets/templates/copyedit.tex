%!TEX TS-program = xelatex
\documentclass[12pt]{article}

\input{../.assets/templates/_common}
\input{../.assets/templates/_pandoc}

\geometry{verbose,letterpaper,tmargin=2.5cm,bmargin=2.5cm,lmargin=2.5cm,rmargin=4.5cm}
\usepackage{lineno}
\usepackage[nolists,noheads]{endfloat}

\pagestyle{plain}

\begin{document}

{\Large\bfseries ${ title }}
\vskip 5em
Last revision: \emph{\today}
\vskip 5em
${ if(description) }
\textbf{Abstract: }${ description }
${ endif }

\clearpage

\section{Authors}

{\small
${ for(authors) }
${ it.givennames }\,${ it.familyname }\,%
(${ for(it.affilcode) }${ it.affilcode }${ sep },${ endfor }${ if(it.email) },*${ endif })\\
${ endfor }
}

\section{Affiliations}

{\small
${ for(institutions) }
(${ it.id })\,\emph{${ it.name }}\\
${ endfor }
${ for(authors) }
${ if(it.email) }
(*)\,\texttt{${ it.email }}\\
${ endif }
${ endfor }
}

\clearpage
\linenumbers
\doublespacing

${ body }

\end{document}
