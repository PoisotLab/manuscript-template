${ if(it.regular) }
${ it.content }
${ endif }

${ if(it.itemized) }
\begin{enumerate}
${ for(it.content) }
    \item${ it }%
${ endfor }
\end{enumerate}
${ endif }

${ if(it.structured) }
    ${ for(it.content) }
    {\bfseries ${ it.key }}\hfill 2em ${ it.value }\\%
    ${ endfor }
${ endif }
