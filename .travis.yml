dist: xenial
sudo: false
git:
  quiet: true

addons:
  apt:
    update: true

before_install:
  - sudo apt-get install pandoc -y
  - sudo apt-get install pandoc-citeproc -y
  - git clone https://github.com/tpoisot/barlock.git
  - chmod u+x barlock/install.sh

script:
  - ./barlock/install.sh
